import { defineCloudflareConfig } from "@opennextjs/cloudflare";

// Basic configuration for OpenNext on Cloudflare
// Bundle size optimization is primarily handled in wrangler.jsonc and next.config.ts
export default defineCloudflareConfig({});
